import { ProductFortunePipe } from './product-fortune.pipe';

describe('ProductFortunePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductFortunePipe();
    expect(pipe).toBeTruthy();
  });
});
